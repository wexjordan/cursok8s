version: "3.8"

services:
  cursok8s:
    build:
      context: .
      dockerfile: Dockerfile
    image: wexjordan/cursok8s:local
    container_name: cursok8s
    ports:
      - "8086:80"
    restart: unless-stopped
